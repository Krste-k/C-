@using SEDC.PizzaApp.Models.ViewModels;
@model List<OrderViewModels>

	<ol>
	@for(int i = 0; i < Model.Count; i++)
	{
		<li>@Model.ElementAt(i).PizzaNames - @Model.ElementAt(i).UserFullName - @Model.ElementAt(i).Price</li>
	}
</ol>